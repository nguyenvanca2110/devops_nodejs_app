apiVersion: v1
kind: Service
metadata:
    name: "canvaksdemo-c908"
    labels:
        app: "canvaksdemo-c908"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "canvaksdemo-c908"